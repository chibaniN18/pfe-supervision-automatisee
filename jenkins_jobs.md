# Jobs Jenkins - Configuration

## Fix-Docker
```shell
ansible-playbook /etc/ansible/playbooks/fix_docker.yml
```

## Fix-Disk
```shell
ansible-playbook /etc/ansible/playbooks/fix_disk.yml
```

## Fix-Memory
```shell
ansible-playbook /etc/ansible/playbooks/fix_memory.yml
```

## Fix-CPU
```shell
ansible-playbook /etc/ansible/playbooks/fix_cpu.yml
```
